#include "main.h"

/**
 * add - adds two numbers
 * @x: accept the first number
 * @y: accept the second number
 * Return: z the total number
 */
int add(int x, int y)
{
	int z = x + y;

	return (z);
}
