#include <stdio.h>

/**
 * main - Description: print all single digit 
 * numbers of base 10 starting from 0
 * Return 0
 */
int main(void)
{
	int i;
	for(i = 0; i<10; i++)
		printf("%d",i);

	return (0);
}
